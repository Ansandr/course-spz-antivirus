Repo isn't initialized yet, and I can see how the files map to your stories. One structural fact that shapes the plan: mainwindow.h (declaration) and mainwindow.cpp (full implementation, includes all 5 tabs) is a single integrated unit, and several stories (US‑05, US‑11, US‑12, US‑13, US‑14) have no dedicated source file. So I'll split along the natural .h`/.cpp` boundary and use empty milestone commits (`--allow-empty`) for the stories that are verification/docs only — a clean, honest way to keep all 14 stories visible in history.

Note: your .gitignore ignores docs, so the записка/скріншоти won't be tracked (that's why US‑13/US‑14 are milestone commits).

Here is the full sequence to run one after another in your Git Bash terminal.

### 0. Init + Gitflow base
git init -b main
git remote add origin https://github.com/Ansandr/course-spz-antivirus.git
git add README.md LICENSE .gitignore .github/
git commit -m "chore: initial repository skeleton (README, LICENSE, .gitignore)"
git branch develop

### Sprint 1 — Ядро системи
# US-01 — CMake / project setup
git checkout -b feature/project-setup develop
git add CMakeLists.txt
git commit -m "build: CMake with antivirus_core static lib + AntivirusMVP target (US-01)"
git checkout develop
git merge --no-ff feature/project-setup -m "Merge feature/project-setup (US-01)"

# US-02 — Logger
git checkout -b feature/logger develop
git add src/logger.h src/logger.cpp
git commit -m "feat(logger): thread-safe singleton Logger, INFO/WARNING/ERROR (US-02)"
git checkout develop
git merge --no-ff feature/logger -m "Merge feature/logger (US-02)"

# US-03 — SignatureDB
git checkout -b feature/signaturedb develop
git add src/signaturedb.h src/signaturedb.cpp signatures.db
git commit -m "feat(signaturedb): load MD5 signatures into QHash, O(1) lookup (US-03)"
git checkout develop
git merge --no-ff feature/signaturedb -m "Merge feature/signaturedb (US-03)"

# US-04 + US-05 — Scanner і консольна верифікація EICAR
git checkout -b feature/scanner develop
git add src/scanner.h src/scanner.cpp
git commit -m "feat(scanner): recursive walk + MD5 hashing in QThread (US-04)"
git commit --allow-empty -m "test(scanner): verify EICAR detection in console mode (US-05)"
git checkout develop
git merge --no-ff feature/scanner -m "Merge feature/scanner (US-04, US-05)"

### Sprint 2 — GUI та інтеграція
# US-06 — каркас MainWindow (декларація + точка входу)
git checkout -b feature/mainwindow-skeleton develop
git add src/main.cpp src/mainwindow.h
git commit -m "feat(gui): MainWindow skeleton with QTabWidget (5 tabs) (US-06)"
git checkout develop
git merge --no-ff feature/mainwindow-skeleton -m "Merge feature/mainwindow-skeleton (US-06)"

# US-07 — реалізація вкладки Сканування (mainwindow.cpp)
git checkout -b feature/scan-tab develop
git add src/mainwindow.cpp
git commit -m "feat(gui): Scan tab with QThread, QProgressBar, results table (US-07)"
git checkout develop
git merge --no-ff feature/scan-tab -m "Merge feature/scan-tab (US-07)"

# US-08 — ProcessList
git checkout -b feature/processlist develop
git add src/processlist.h src/processlist.cpp
git commit -m "feat(processlist): process snapshot via WinAPI Toolhelp32 (US-08)"
git checkout develop
git merge --no-ff feature/processlist -m "Merge feature/processlist (US-08)"

# US-09 — Quarantine
git checkout -b feature/quarantine develop
git add src/quarantine.h src/quarantine.cpp
git commit -m "feat(quarantine): isolate/restore with JSON index metadata (US-09)"
git checkout develop
git merge --no-ff feature/quarantine -m "Merge feature/quarantine (US-09)"

### Sprint 3 — Завершення та тестування
```bash
# US-10 — ReportWriter
git checkout -b feature/reports develop
git add src/reportwriter.h src/reportwriter.cpp
git commit -m "feat(reports): auto-save scan report to timestamped txt (US-10)"
git checkout develop
git merge --no-ff feature/reports -m "Merge feature/reports (US-10)"

# US-11 — вкладки Журнал/Налаштування (вже в mainwindow.cpp -> milestone)
git checkout -b feature/log-settings-tabs develop
git commit