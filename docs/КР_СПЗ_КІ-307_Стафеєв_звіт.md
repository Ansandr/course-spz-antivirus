Міністерство освіти і науки України

НАЦІОНАЛЬНИЙ УНІВЕРСИТЕТ «ЛЬВІВСЬКА ПОЛІТЕХНІКА»

Кафедра ЕОМ

![](media/image1.jpeg){width="3.9286187664041994in"
height="3.727273622047244in"}

КУРСОВИЙ ПРОЕКТ

з дисципліни «Системне програмне забезпечення»

на тему: «Розробка програмного забезпечення для захисту від вірусів та
шкідливих програм»

Варіант № 33

Студента 3-го курсу групи:

КІ-304

123 «Комп'ютерна інженерія»

Стафеєв А. А.

Керівник

доц. каф. ЕОМ

Олексів М. В.

Львів - 2026

[]{#_Toc231256447 .anchor}Завдання на курсовий проект

[]{#_Toc231256448 .anchor}Анотація

У курсовому проекті розроблено програмне забезпечення для захисту
персональних комп\'ютерів від вірусів та шкідливих програм -
антивірусний сканер «Antivirus MVP» для операційної системи Windows.
Робота виконана за результатами аналізу п\'яти існуючих рішень (Windows
Security, ESET, ClamWin, rkhunter, System Informer), що дозволив виявити
дефіцит легких, прозорих та контрольованих користувачем інструментів
захисту.

Програму реалізовано мовою C++ 17 із застосуванням фреймворку Qt 6
Widgets за модульною архітектурою: низькорівневе ядро бізнес-логіки
оформлене у вигляді статичної бібліотеки, рівень представлення --- у
вигляді GUI-застосунку. Реалізовано сигнатурне сканування файлів на
основі MD5-хешування (QCryptographicHash, QDirIterator), перегляд
активних процесів системи через Windows API (CreateToolhelp32Snapshot),
ізоляцію виявлених загроз до карантину, генерацію текстових звітів,
потоко-безпечне журналювання та підтримку білого списку виключень.
Тривалі операції виконуються в окремому потоці (QThread), що забезпечує
реагуючий інтерфейс.

Розробку організовано за методологією Scrum (14 User Stories, 3 спринти,
19 Story Points) із використанням системи контролю версій Git за
стратегією Gitflow. Працездатність підтверджено набором із 12
тест-кейсів, що покривають функціональні та нефункціональні вимоги;
коректність виявлення загроз перевірено за допомогою стандартного
тестового файлу EICAR.

[]{#_Toc231256449 .anchor}Зміст

[Завдання на курсовий проект [2](#_Toc231256447)](#_Toc231256447)

[Анотація [3](#_Toc231256448)](#_Toc231256448)

[Зміст [4](#_Toc231256449)](#_Toc231256449)

[Вступ [5](#_Toc231256450)](#_Toc231256450)

[Розділ 1. Аналіз інсуючих рішень та методів захисту від шкідливого ПЗ
[6](#_Toc231256451)](#_Toc231256451)

[Розділ 2. Проектування [7](#_Toc231256452)](#_Toc231256452)

[Розділ 3. Реалізація
[8](#макет-інтерфейсу-користувача-wireframe)](#макет-інтерфейсу-користувача-wireframe)

[Розділ 4. Тестування на інстркція користувача
[9](#Xcedc4a694333a16d765df153879a565d7d44cfa)](#Xcedc4a694333a16d765df153879a565d7d44cfa)

[Висновки [10](#_Toc231256455)](#_Toc231256455)

[Висновки [11](#_Toc231256456)](#_Toc231256456)

[Список використаної літератури [12](#_Toc231256457)](#_Toc231256457)

[Додатки [13](#_Toc231256458)](#_Toc231256458)

[]{#_Toc231256450 .anchor}Вступ

1.  []{#_Toc231256451 .anchor}Аналіз інсуючих рішень та методів захисту
    від шкідливого ПЗ

## Огляд методів виявлення шкідливого програмного забезпечення

Проблема захисту від шкідливого ПЗ вирішується багатошаровим гібридним
підходом, оскільки жоден окремий метод не забезпечує достатню
ефективність. Основні методи:

Сигнатурний аналіз - порівняння файлів із базою відомих вірусних
сигнатур. Переваги: швидкість, точність. Недоліки: неефективний проти
невідомих загроз.

Евристичний аналіз - розпізнавання підозрілого поведінкового коду на
основі статичного аналізу. Виявляє поліморфні віруси, що видозмінюють
себе.

Поведінковий моніторинг - відстеження дій процесів у реальному часі
(читання файлів, системні виклики, мережева активність). Дозволяє
блокувати загрози нульового дня.

Хмарна репутація - централізований аналіз невідомих файлів та їх оцінка
на основі глобальної телеметрії мільйонів комп'ютерів.

## Аналіз існуючих програмних аналогів

Для обґрунтування необхідності розробки було проаналізовано п\'ять
різних рішень для захисту від шкідливого ПЗ, що представляють різні
підходи та цільові аудиторії.

### Вбудований захист Windows Security

Опис: Вільний, інтегрований у Windows 10/11 антивірус корпорації
Microsoft. Використовує сигнатури, евристику, машинне навчання та
компонент Smart App Control для оцінки репутації програм.

Переваги: Безплатний, безшовна інтеграція з ОС, мінімум конфліктів,
постійні оновлення.

Недоліки: Smart App Control блокує безпечні портативні програми без
дорогих цифрових сертифікатів; слабкість механізму Tamper Protection;
монополізація системних ресурсів Antimalware Service Executable.

### Програмний комплекс ESET

Опис: Комерційне преміум-рішення з евристичним аналізом та технологією
LiveGrid для хмарної репутації файлів.

Переваги: Висока швидкість реакції на нові загрози, низьке споживання
ресурсів, комплексний інструментарій (шифрування, батьківський
контроль).

Недоліки: Критична проблема --- SSL-фільтрація за замовчуванням розриває
з\'єднання npm, pip та Git через самопідписаний сертифікат; надмірна
підозрілість до системних утиліт.

### Антивірусний сканер ClamWin

Опис: Безплатний open-source сканер на основі рушія ClamAV. Класичний
сигнатурний аналіз без резидентного моніторингу.

Переваги: Повна прозорість вихідного коду (GPL v2), нульовий вплив на
продуктивність, працює на старих системах (Windows 98/XP).

Недоліки: Відсутність моніторингу у реальному часі; архаїчний інтерфейс;
повільне повне сканування; слаба евристика проти поліморфних загроз.

### Сканер rkhunter

Опис: Спеціалізована утиліта для виявлення руткітів та бекдорів.
CLI-інтерфейс, криптографічна верифікація системних файлів. Розроблена
для UNIX/Linux.

Переваги: Унікальна здатність виявляти ядерні загрози; глибокий аудит
цілісності системи; открытий код, портативність.

Недоліки: Відсутність графічного інтерфейсу; велика кількість хибних
спрацьовувань при оновленнях ОС; вузька спеціалізація не замінює
класичний антивірус.

### Утиліта моніторингу System Informer

Опис: Безплатна open-source утиліта (раніше Process Hacker) для
низькорівневого моніторингу процесів та ручного аналізу динамічної
активності системи.

Переваги: Феноменальна деталізація поведінки ОС; незамінна для
дослідження інцидентів; повна прозорість та портативність.

Недоліки: Повна відсутність автоматизації --- користувач сам вирішує,
який процес небезпечний; надмірна складність інтерфейсу; часто
блокується антивірусами як підозрілий.

## Обґрунтування мети та завдань курсового проєкту

Аналіз п\'яти рішень виявив системну проблему сучасної антивірусної
індустрії: кожен інструмент проектується як моноліт з заздалегідь
визначеною філософією, жертвуючи гнучкістю заради спеціалізації.

Кумулятивні недоліки аналогів:

- Автоматичні системи (Windows Security, ESET) перевищують контроль
  користувача й блокують легітимний код;

- Open-source рішення (ClamWin) не мають моніторингу у реальному часі;

- Спеціалізовані інструменти (rkhunter, System Informer) недоступні для
  рядових користувачів (CLI, відсутність автоматизації);

- Всі рішення постійно генерують хибні спрацьовування або ускладнюють
  легітимну роботу розробників;

- Немає простого, легкого, прозорого рішення, що поєднує переваги.

Запропоноване рішення: Розробити легкий, модульний антивірусний сканер з
ясним UI, що поєднує:

- Сигнатурний аналіз з динамічним завантаженням баз;

- Евристичний модуль для виявлення поліморфних загроз;

- Моніторинг активних процесів у реальному часі;

- Мінімальне споживання ресурсів;

- Повна прозорість механізмів рішень (log-файли, деталізація);

- Ручний контроль користувача над автоматизацією.

2.  []{#_Toc231256452 .anchor}Проектування

## Технічне завдання

### Найменування та область застосування

**Назва проекту:** Модульна система виявлення та моніторингу шкідливого
програмного забезпечення на базі Windows.

**Область застосування:** Розробка легкого, контрольованого
антивірусного сканера для персональних комп\'ютерів з операційною
системою Windows 10/11, призначеного як для рядових користувачів, так і
для системних адміністраторів та розробників ПЗ.

2.   **Мета та завдання розробки**

**Мета:** Створити прозорий, легкий антивірусний сканер з графічним
інтерфейсом на базі Qt/C++, що реалізує сигнатурне сканування файлів,
перегляд активних процесів та ізоляцію загроз у карантин, з повним
контролем користувача над всіма діями програми.

**Основні завдання:** 1. Розробити модуль сигнатурного аналізу: обхід
директорії, обчислення MD5-хешу кожного файлу та порівняння з локальною
базою signatures.db; 2. Реалізувати вікно перегляду активних процесів
системи (PID, ім'я, шлях до бінарника); 3. Розробити модуль карантину:
переміщення виявлених загроз до ізольованої директорії з можливістю
відновлення або видалення; 4. Розробити графічний інтерфейс (Qt Widgets)
з вкладками: Сканування, Процеси, Карантин, Журнал; 5. Реалізувати
логування всіх операцій програми у файл antivirus.log; 6. Підготувати
демонстраційну базу сигнатур з EICAR-тестовим файлом.

3.  **Характеристика предметної області та обґрунтування необхідності
    розробки**

За результатами аналізу п'яти провідних рішень виявлено критичний
дефіцит на ринку: - Комерційні антивірусні комплекси (ESET, Windows
Security) обмежують контроль користувача або порушують працездатність
легітимних засобів розробки; - Open-source рішення (ClamWin) не мають
моніторингу у реальному часі та виглядають архаїчно; - Спеціалізовані
інструменти (rkhunter, System Informer) недоступні для рядових
користувачів або потребують глибоких технічних знань.

Запропоноване рішення має заповнити цю нішу збалансованим, прозорим,
контрольованим інструментом з можливостями професійного рівня та
простотою використання для звичайних користувачів.

4.  **Функціональні вимоги до системи**

    1.  Функційність модуля сканування

**ВР1-Сканування:** Програма повинна виконувати сканування обраної
директорії з порівнянням MD5-хешів файлів з локальною базою сигнатур. -
Детально: Через кнопку "Обрати директорію" (стандартний QFileDialog)
користувач вибирає папку; запускається рекурсивний обхід (QDirIterator);
для кожного файлу обчислюється MD5-хеш через QCryptographicHash; хеш
порівнюється з signatures.db; результати (чисто/загроза) відображаються
у таблиці QTableWidget; прогрес показує QProgressBar; після завершення
--- рядок зі статистикою (перевірено/знайдено загроз).

**ВР2-Карантин:** Програма повинна переміщувати виявлені загрози до
ізольованої директорії карантину. - Детально: При виявленні загрози та
підтвердженні користувача програма переміщує файл через QFile::rename()
до директорії %APPDATA%\\AntivirusMVP\\Quarantine\\; у вкладці
"Карантин" відображається список ізольованих файлів (ім'я, оригінальний
шлях, дата ізоляції); для кожного запису доступні кнопки "Відновити" та
"Видалити назавжди".

**ВР3-Перегляд процесів:** Програма повинна відображати список всіх
активних процесів системи. - Детально: Вкладка "Процеси" показує таблицю
з колонками: PID, Ім'я процесу, Шлях до виконуваного файлу; список
отримується через Windows API CreateToolhelp32Snapshot() /
Process32First() / Process32Next(); кнопка "Оновити" оновлює список; для
підозрілих процесів (не мають цифрового підпису або шлях у Temp)
підсвічується рядок червоним кольором.

**ВР4-Звітування:** Програма повинна записувати результати сканування у
текстовий файл. - Детально: Після завершення сканування програма
автоматично зберігає звіт у
%APPDATA%\\AntivirusMVP\\Reports\\scan_YYYYMMDD_HHMMSS.txt; звіт
містить: дату/час, кількість проаналізованих файлів, список виявлених
загроз з повними шляхами та MD5-хешами; кнопка "Відкрити звіт" відкриває
файл у стандартному текстовому редакторі.

**ВР5-Білий список:** Програма повинна підтримувати список виключень зі
сканування. - Детально: У вкладці "Налаштування" є текстове поле для
додавання шляхів-виключень (по одному на рядок); список зберігається у
%APPDATA%\\AntivirusMVP\\whitelist.txt; перед скануванням кожного файлу
перевіряється, чи його шлях не входить до виключень --- якщо так, файл
пропускається.

**ВР6-Журналювання:** Програма повинна вести журнал усіх операцій у
текстовий файл. - Детально: Усі події (запуск сканування, знаходження
загрози, переміщення до карантину, помилки читання файлів) записуються
до %APPDATA%\\AntivirusMVP\\antivirus.log з часовою міткою та рівнем
(INFO / WARNING / ERROR); вкладка "Журнал" завантажує та відображає
вміст файлу через QPlainTextEdit; реалізується через singleton-клас
Logger.

2.  2.1.4.2. Функційність модуля інтерфейсу

**ВР7-Графічний інтерфейс:** Програма повинна мати зрозумілий графічний
інтерфейс на Qt Widgets з чотирма вкладками. - Детально: Головне вікно
QMainWindow містить QTabWidget з вкладками: "Сканування" (вибір
директорії, прогрес, таблиця результатів), "Процеси" (таблиця активних
процесів), "Карантин" (список ізольованих файлів), "Журнал" (текстове
поле з логом) та "Налаштування" (білий список); всі тривалі операції
виконуються у QThread, щоб UI залишався реагуючим.

5.  **Нефункціональні вимоги до системи**

    1.  Вимоги до продуктивності

**НВ1-Швидкість сканування:** Час сканування одного типового файлу
(розмір 1 МБ) не повинен перевищувати 50 мс. - Обґрунтування: Дозволяє
завершити повне сканування типової домашньої папки (100 ГБ, \~100 тис.
файлів) протягом \~1.5 години при фоновому режимі.

**НВ2-Утилізація CPU:** Під час фонового сканування програма не повинна
утилізувати більше 25% CPU однієї ядра. - Обґрунтування: Не перешкоджає
основній роботі користувача при відкритих браузерах, редакторах та інших
застосунків.

**НВ3-Утилізація пам'яті:** Резидентна утилізація оперативної пам'яті не
повинна перевищувати 150 МБ у звичайному режимі. - Обґрунтування:
Залишає достатньо ресурсів для комфортної роботи користувача на системах
з 8 ГБ оперативної пам'яті.

**НВ4-Час відповіді інтерфейсу:** Час відповіді на дії користувача
(клацання кнопок, прокручування) не повинен перевищувати 100 мс. -
Обґрунтування: Забезпечує плавний та реагуючий користувацький досвід.

2.  2.1.5.2. Вимоги до надійності та безпеки

**НВ5-Надійність:** Програма не повинна вихід з ладу при обробці
пошкоджених або аномальних файлів. - Обґрунтування: Повинна мати обробку
виключень для всіх потенційно небезпечних операцій (читання файлів,
парсинг структур); критична помилка не повинна призвести до краху
програми.

**НВ6-Захист від втручання:** Программа повинна захищатися від спроб
вирусів видалити або модифікувати її файли. - Обґрунтування: Виконуваний
файл програми та файли конфігурації повинні встановлюватися з
обмеженнями доступу; мають перевірятися цифрові підписи всіх критичних
компонентів.

**НВ7-Конфіденційність:** Програма не повинна збирати та передавати дані
про користувача без його явної згоди. - Обґрунтування: Всі операції
сканування, логування та оновлення баз виконуються локально; хмарні
функції (якщо реалізовані) активуються тільки після явного підтвердження
користувачем.

3.  Вимоги до операційного середовища

**НВ8-Операційна система:** Програма повинна працювати на Windows 10
версії 2004 та новішої, а також Windows 11 всіх версій. - Обґрунтування:
Охоплює 95% встановлених копій Windows на світі (станом на 2026 рік).

**НВ9-Мінімальні технічні вимоги:** - Процесор: Intel Core i3 або
еквівалент AMD, 2 ядра; - ОЗУ: мінімум 4 ГБ; - Вільне місце на диску:
мінімум 500 МБ; - Мережевий доступ: для завантаження оновлень баз.

**НВ10-Мови програмування та технології:** - **Мова реалізації:** C++
17; - **Фреймворк GUI та утиліти:** Qt 6 (Qt Widgets, QFileDialog,
QThread, QCryptographicHash, QDirIterator, QJsonDocument); - **Windows
API:** tlhelp32.h --- для отримання списку процесів
(CreateToolhelp32Snapshot); - **Формат бази сигнатур:** плаский
текстовий файл signatures.db (MD5-хеш + ім'я загрози, одна на рядок); -
**Інструменти білду:** CMake 3.20+ з Qt6 CMake-інтеграцією або Qt
Creator (qmake); - **Система керування версіями:** Git з Gitflow
стратегією; - **Тестування:** Qt Test (вбудований фреймворк Qt для
unit-тестів).

6.  **Допущення та обмеження**

    1.  Допущення

- Користувач має права адміністратора на своєму комп'ютері для
  встановлення програми та запуску драйверів;

- Комп'ютер має стабільне мережне з'єднання для завантаження оновлень
  баз (мінімум 1 Мбіт/с);

- База вірусних сигнатур розпорядника буде оновлюватися щомісяця з новим
  контентом;

- Не враховуються атаки користувача з правами TrustedInstaller або
  спеціальні техніки ескалації привілеїв;

- Програма розроблюється як рішення для захисту кінцевих точок (endpoint
  protection), а не для централізованого корпоративного управління.

  1.  Обмеження

- **Функціональність за межами проекту (виключена зі скоупу MVP):**

  - Автоматичне планування сканувань за розкладом --- відсутній
    планувальник задач;

  - Блокування запуску підозрілих процесів у реальному часі ---
    відсутній драйвер рівня ядра (minifilter);

  - Мережеве оновлення бази сигнатур --- база оновлюється вручну заміною
    файлу;

  - Інтеграція з контекстним меню Windows Explorer;

  - Евристичний аналіз вмісту файлів (тільки хеш-порівняння);

  - Інтеграція з централізованими системами керування (SIEM, EDR);

  - Мобільна версія для Android або iOS;

  - Міжплатформна портативність (Linux, macOS).

- **Технічні обмеження:**

  - Програма не може запуститися на пошкоджених або зараженим системах,
    де критичні компоненти ОС вже скомпрометовані;

  - Не гарантується виявлення 100% сучасних руткітів, що мають більш
    глибоку інтеграцію з ядром, ніж сама програма;

  - Евристичний аналіз базується на статичних ознаках файлу, без
    використання динамічної пісочниці (sandbox) або машинного навчання.

- **Вимоги до персоналу:**

  - Адміністратор/розробник повинен розумітися на основах Windows API та
    архітектури процесів;

  - Для налаштування конфіг-файлів потрібна базова грамотність у роботі
    з текстовими файлами та реєстром Windows.

    1.  **Ризики та план мітигації**

  -------------------------------------------------------------------------------------------
  \#    Ризик              Ймовірність         Вартість        Мітигація
  ----- ------------------ ------------------- --------------- ------------------------------
  Р1    Затримання доступу Середня             Низька          Перехоплення виключення
        до файлів, які                                         QFile::open() з виведенням
        використовуються                                       попередження; файл
        іншими процесами                                       пропускається та позначається
                                                               у звіті як «недоступний»

  Р2    Хибно-позитивні    Низька              Середня         Використання додаткової
        збіги хешів через                                      верифікації імені файлу поруч
        колізії MD5                                            з хешем; документування
                                                               обмеження у звіті

  Р3    Переповнення диску Низька              Середня         Попередня перевірка вільного
        файлами карантину                                      місця перед переміщенням;
                                                               виведення попередження якщо
                                                               місця менше 100 МБ

  Р4    Некоректне         Середня             Низька          Обробка ERROR_ACCESS_DENIED
        відображення                                           при
        шляхів процесів                                        QueryFullProcessImageName();
        (відсутність прав                                      відображення «N/A» замість
        доступу)                                               краху
  -------------------------------------------------------------------------------------------

2.  **Етапи розробки та план проекту (MVP)**

  ---------------------------------------------------------------------------
  Етап         Блок             Завдання              Результат
  ------------ ---------------- --------------------- -----------------------
  Етап 1       Налаштування +   Ініціалізація         Консольна версія:
               Ядро             Qt-проєкту,           сканер знаходить
                                CMakeLists, структура EICAR-тестовий файл
                                директорій; клас      
                                Logger; клас          
                                SignatureDB           
                                (завантаження         
                                signatures.db); клас  
                                Scanner (рекурсивний  
                                обхід + MD5)          

  Етап 2       GUI + Процеси +  MainWindow з          Повністю інтерактивний
               Карантин         QTabWidget; вкладка   GUI; сканування,
                                "Сканування" (кнопка, перегляд процесів,
                                прогрес-бар,          переміщення до
                                таблиця); QThread для карантину
                                сканування; вкладка   
                                "Процеси" (Windows    
                                API); клас            
                                Quarantine + вкладка  
                                "Карантин"            

  Етап 3       Завершення +     Вкладка "Журнал" та   Робоча програма, файл
               Тестування       "Налаштування" (білий звіту scan\_\*.txt,
                                список); генерація    звіт про тестування,
                                звіту .txt (клас      фінальний коміт
                                ReportWriter);        
                                тестування на EICAR;  
                                фіксація дефектів;    
                                скріншоти для записки 
  ---------------------------------------------------------------------------

Структура проекту Qt:

*Рис. 2.1. Структурна схема модулів антивірусного сканера*

Загальна тривалість розробки MVP: 3 етапи

1.  **Структурна схема програми**

    1.  **Опис архітектури системи**

Архітектура програми побудована за **модульним принципом**: кожен
функціональний блок реалізований як окремий C++-клас з чітко визначеним
інтерфейсом і мінімальними залежностями. Взаємодія між модулями
відбувається через сигнали та слоти Qt (механізм publisher-subscriber).
Весь рівень представлення зосереджений у класі MainWindow, що виступає
єдиною точкою входу для дій користувача.

Система складається з двох архітектурних рівнів: - **Низькорівневий
рівень (ядро)** --- C++ класи Scanner, SignatureDB, Quarantine,
ProcessList, Logger, ReportWriter. Компілюються як статична бібліотека
antivirus_core.lib, що забезпечує незалежність бізнес-логіки від GUI. -
**Рівень представлення (GUI)** --- клас MainWindow на Qt Widgets, що
підключає бібліотеку ядра та відображає результати.

2.  **Структурна схема модулів**

![](media/image2.jpeg){width="1.9478269903762029in"
height="1.8479997812773403in"}

Рис. 2.1. Структурна схема модулів антивірусного сканера

3.  **Опис модулів системи**

  ------------------------------------------------------------------------
  Модуль             Клас           Відповідальність
  ------------------ -------------- --------------------------------------
  Сканер             Scanner        Рекурсивний обхід директорії через
                                    QDirIterator; обчислення MD5-хешу
                                    кожного файлу через
                                    QCryptographicHash; перевірка по
                                    SignatureDB та whitelist; генерація
                                    сигналу з результатом для GUI

  База сигнатур      SignatureDB    Завантаження та парсинг signatures.db;
                                    зберігання хешів у QHash\<QString,
                                    QString\> (хеш → назва загрози); метод
                                    lookup(hash)

  Карантин           Quarantine     Переміщення файлів через
                                    QFile::rename(); зберігання метаданих
                                    (оригінальний шлях, дата) у
                                    quarantine_index.json; відновлення та
                                    видалення

  Список процесів    ProcessList    Виклик CreateToolhelp32Snapshot() +
                                    QueryFullProcessImageName(); повертає
                                    QVector\<ProcessInfo\> з полями: PID,
                                    ім'я, шлях, підозрілість

  Журнал             Logger         Singleton; запис рядків з часовою
                                    міткою та рівнем (INFO/WARNING/ERROR)
                                    до файлу через QTextStream;
                                    thread-safe через QMutex

  Звіти              ReportWriter   Формування текстового файлу зі
                                    статистикою сканування та списком
                                    загроз; назва файлу з часовою міткою

  Головне вікно      MainWindow     Управляє QTabWidget; запускає Scanner
                                    у QThread; відображає результати у
                                    QTableWidget; з'єднує сигнали ядра зі
                                    слотами GUI
  ------------------------------------------------------------------------

2.  **Вибір засобів реалізації**

    1.  **Вибір мови програмування**

Для даного курсового проєкту порівнювалися три мови системного рівня:

  -------------------------------------------------------------------------
  Критерій             C++ 17           Rust         C# (.NET)
  -------------------- ---------------- ------------ ----------------------
  Доступ до Windows    Нативний         Через крейти P/Invoke (непряме)
  API                  (windows.h)      (winapi)     

  Управління пам'яттю  Ручне (RAII)     Власність    GC (непередбачувані
                                        (borrow      паузи)
                                        checker)     

  Підтримка Qt         Рідна мова Qt    Обмежена     Відсутня
                                        (qt-rs)      

  Швидкість компіляції Середня          Повільна     Висока

  Поріг входження      Середній         Високий      Низький

  Зрілість екосистеми  Висока (30+      Середня      Висока
                       років)                        
  -------------------------------------------------------------------------

**Обрано: C++ 17.** Обґрунтування: Qt написано на C++ і надає весь
необхідний інструментарій нативно. Нативний доступ до tlhelp32.h без
прошарків. RAII та розумні покажчики (std::unique_ptr, std::shared_ptr)
забезпечують безпечне управління ресурсами без GC-пауз.

2.  **Вибір фреймворку GUI**

  ------------------------------------------------------------------------------
  Критерій             Qt 6 Widgets            wxWidgets             GTK+
  -------------------- ----------------------- --------------------- -----------
  Кросплатформеність   Windows/Linux/macOS     Windows/Linux/macOS   Переважно
                                                                     Linux

  Якість документації  Відмінна                Середня               Середня

  Вбудована підтримка  QThread, QtConcurrent   Потребує зовнішніх    Потребує
  потоків                                      libs                  glib

  Hashing/Crypto       QCryptographicHash      Зовнішня lib          Зовнішня
                       вбудований                                    lib

  Ліцензія             LGPL v3 (комерційна та  wxWindows License     LGPL
                       open-source)                                  

  Designer             Qt Designer (.ui XML)   Відсутній             Glade
  (UI-редактор)                                                      
  ------------------------------------------------------------------------------

**Обрано: Qt 6.** Обґрунтування: QCryptographicHash (MD5), QDirIterator,
QThread, QFileDialog --- всі необхідні компоненти вбудовані без
сторонніх залежностей. Qt Designer прискорює розробку GUI. Ліцензія LGPL
дозволяє використання у навчальних проєктах.

3.  **Інструменти збірки та середовище розробки**

- **Збірка:** CMake 3.20+ з find_package(Qt6) --- стандарт для C++
  проєктів, підтримується VS Code та CLion.

- **IDE:** Qt Creator або VS Code з розширенням CMake Tools та clangd.

- **Компілятор:** MSVC 2022 або MinGW-w64 (GCC 13).

- **Контроль версій:** Git + Gitflow; гілки feature/scanner-core,
  feature/gui, feature/quarantine.

- **Тестування:** Qt Test (вбудований) для unit-тестів класів
  SignatureDB та Scanner.

  1.  **Проектування**

      1.  **UML-діаграма класів**

![](media/image3.png){width="6.0in" height="4.836734470691163in"}

Рис. 2.2. UML-діаграма класів антивірусного сканера

2.  **Алгоритм сканування файлової системи**

![](media/image4.png){width="2.4084755030621174in"
height="8.044391951006125in"}

Рис. 2.3. Алгоритм роботи модуля сканування

3.  **Алгоритм ізоляції файлу до карантину**

![](media/image5.png){width="6.0in" height="8.869897200349957in"}

Рис. 2.4. Алгоритм ізоляції виявленої загрози до карантину

### Макет інтерфейсу користувача (Wireframe)

Головне вікно програми QMainWindow (800×600 пікселів) містить QTabWidget
з п'ятьма вкладками:

**Вкладка «Сканування»:**

    ┌─────────────────────────────────────────────────────┐
    │  [Сканування] [Процеси] [Карантин] [Журнал] [Налаш.]│
    ├─────────────────────────────────────────────────────┤
    │  Директорія: [C:\Users\...\Documents    ] [Обрати…] │
    │                                                     │
    │  [▶ Розпочати сканування]          [⏹ Зупинити]     │
    │                                                     │
    │  ████████████░░░░░░░░░  64 / 100 файлів             │
    │                                                     │
    │  ┌──────────────────────────────────────────────┐   │
    │  │ Файл                │ Статус   │ Загроза      │   │
    │  ├─────────────────────┼──────────┼─────────────┤   │
    │  │ C:\...\doc.exe      │ ✅ Чисто  │ —           │   │
    │  │ C:\...\virus.exe    │ ⚠ Загроза│ EICAR-Test  │   │
    │  └──────────────────────────────────────────────┘   │
    │                                                     │
    │  Перевірено: 64  Загроз: 1          [Відкрити звіт] │
    └─────────────────────────────────────────────────────┘

**Вкладка «Процеси»:**

    ┌─────────────────────────────────────────────────────┐
    │  [Сканування] [Процеси] [Карантин] [Журнал] [Налаш.]│
    ├─────────────────────────────────────────────────────┤
    │  [🔄 Оновити список]                                │
    │  ┌──────────────────────────────────────────────┐   │
    │  │ PID   │ Ім'я процесу    │ Шлях до файлу      │   │
    │  ├───────┼─────────────────┼────────────────────┤   │
    │  │ 1234  │ chrome.exe      │ C:\Program Files\… │   │
    │  │ 5678  │ suspicious.exe  │ C:\Temp\…          │   │  ← червоний
    │  └──────────────────────────────────────────────┘   │
    └─────────────────────────────────────────────────────┘

**Вкладка «Карантин»:**

    ┌─────────────────────────────────────────────────────┐
    │  [Сканування] [Процеси] [Карантин] [Журнал] [Налаш.]│
    ├─────────────────────────────────────────────────────┤
    │  ┌──────────────────────────────────────────────┐   │
    │  │ Ім'я файлу  │ Оригінальний шлях │ Дата       │   │
    │  ├─────────────┼───────────────────┼────────────┤   │
    │  │ virus.exe   │ C:\Temp\virus.exe │ 07.06.2026 │   │
    │  └──────────────────────────────────────────────┘   │
    │                    [↩ Відновити]  [🗑 Видалити]      │
    └─────────────────────────────────────────────────────┘

Рис. 2.5. Макети (wireframe) основних вкладок інтерфейсу

3.  Реалізація

    1.  **Планування розробки (User Stories / Scrum)**

Відповідно до методичних вказівок, розробку програмного забезпечення
організовано за **Scrum**-методологією. Проєкт розбито на **14 User
Stories** (US-01--US-14), розподілених по трьох спринтах відповідно до
плану розділу 2.1.8. Кожна User Story оформлена як окремий Issue у
репозиторії GitHub у форматі «Як \[роль\], я хочу \[дія\], щоб \[мета\]»
з переліком критеріїв виконання, лейблами (core, gui, testing, sprint-N)
та оцінкою у Story Points (1 SP ≈ 0.5 робочого дня). Повний опис всіх
Stories збережено у файлі docs/user-stories.md. Розробка велась у гілках
feature/\* з Merge Request у develop після кожної завершеної Story.

Таблиця 3.1. Зведена таблиця User Stories

  ------------------------------------------------------------------------------
  Спринт   ID      Назва User Story                                     SP
  -------- ------- ---------------------------------------------------- --------
  1        US-01   Ініціалізація Qt-проєкту та CMake                    1

  1        US-02   Клас Logger (Singleton, потоко-безпечне              1
                   журналювання)                                        

  1        US-03   Клас SignatureDB (завантаження та пошук у базі)      1

  1        US-04   Клас Scanner (рекурсивний обхід + MD5 + QThread)     2

  1        US-05   Консольна верифікація виявлення EICAR                1

  2        US-06   MainWindow + QTabWidget (каркас інтерфейсу)          1

  2        US-07   Вкладка «Сканування» з QThread та QProgressBar       2

  2        US-08   Клас ProcessList + вкладка «Процеси» (Windows API)   2

  2        US-09   Клас Quarantine + вкладка «Карантин»                 2

  3        US-10   Клас ReportWriter + автозбереження звіту             1

  3        US-11   Вкладки «Журнал» та «Налаштування» (whitelist)       1

  3        US-12   Unit-тести Qt Test (SignatureDB, Scanner)            1

  3        US-13   Інтеграційне тестування та виправлення дефектів      2

  3        US-14   Скріншоти для записки та фінальний коміт             1

                   **Разом**                                            **19**
  ------------------------------------------------------------------------------

2.  **Структура проекту та конфігурація збірки**

Проєкт організовано відповідно до архітектурного рішення розділу 2.2:
бізнес-логіка компілюється як статична бібліотека antivirus_core,
підключена до GUI-виконуваного файлу AntivirusMVP. Така структура
відповідає вимозі методичних вказівок про наявність низькорівневої
складової окремо від рівня представлення.

**cmake_minimum_required**(VERSION 3.20)\
**project**(AntivirusMVP VERSION 1.0.0 LANGUAGES CXX)\
**set**(CMAKE_CXX_STANDARD 17)\
**set**(CMAKE_AUTOMOC ON)\
\
**find_package**(Qt6 REQUIRED COMPONENTS Core Gui Widgets)\
\
*\# Статична бібліотека ядра (низькорівнева складова)*\
**add_library**(antivirus_core STATIC\
src/logger.cpp src/signaturedb.cpp src/scanner.cpp\
src/quarantine.cpp src/processlist.cpp src/reportwriter.cpp\
)\
**target_include_directories**(antivirus_core PUBLIC src)\
**target_link_libraries**(antivirus_core PUBLIC Qt6::Core psapi)\
\
*\# GUI-виконуваний файл (рівень представлення)*\
**add_executable**(AntivirusMVP WIN32 src/main.cpp src/mainwindow.cpp)\
**target_link_libraries**(AntivirusMVP PRIVATE antivirus_core
Qt6::Widgets)

3.  **Реалізація ядра системи**

    1.  **Модуль Logger**

Клас Logger реалізує Singleton-патерн для централізованого журналювання
всіх подій програми. Потоко-безпека (вимога до архітектури QThread)
забезпечується QMutex через RAII-обгортку QMutexLocker.

*// logger.h (фрагмент)*\
**class** Logger {\
**public**:\
static Logger\* instance();\
bool open(const QString& filePath);\
void info(const QString& msg);\
void warning(const QString& msg);\
void error(const QString& msg);\
**private**:\
static Logger\* s_instance;\
QFile m_logFile;\
QTextStream m_stream;\
QMutex m_mutex; *// захист від race condition*\
};\
\
*// logger.cpp --- метод запису*\
void Logger::write(const QString& level, const QString& message) {\
QMutexLocker lock(&m_mutex); *// RAII-блокування на час запису*\
const QString ts = QDateTime::currentDateTime()\
.toString(\"yyyy-MM-dd HH:mm:ss\");\
m_stream \<\< QString(\"\[%1\] \[%2\] %3\\n\").arg(ts, level, message);\
m_stream.flush();\
}

2.  **Модуль SignatureDB**

Клас SignatureDB завантажує базу сигнатур у QHash\<QString, QString\>
--- хеш-таблицю з O(1)-пошуком. Формат файлу signatures.db: кожен рядок
--- MD5_HEX ThreatName; рядки, що починаються з #, ігноруються як
коментарі.

*// signaturedb.cpp*\
bool SignatureDB::load(const QString& filePath) {\
QFile file(filePath);\
**if** (!file.open(QIODevice::ReadOnly \| QIODevice::Text))\
**return** **false**;\
\
static const QRegularExpression ws(R\"(\\s+)\");\
QTextStream in(&file);\
**while** (!in.atEnd()) {\
const QString line = in.readLine().trimmed();\
**if** (line.isEmpty() \|\| line.startsWith(\'#\')) **continue**;\
const QStringList parts = line.split(ws, Qt::SkipEmptyParts);\
**if** (parts.size() \>= 2)\
m_signatures.insert(parts.at(0).toLower(),\
parts.mid(1).join(\' \'));\
}\
**return** !m_signatures.isEmpty();\
}\
\
QString SignatureDB::lookup(const QString& md5Hex) const {\
**return** m_signatures.value(md5Hex.toLower());\
}

3.  **Модуль Scanner**

Scanner успадковує QObject для роботи з QThread. Рекурсивний обхід
виконується через QDirIterator, MD5-хешування --- через
QCryptographicHash. Атомарний прапор std::atomic\<bool\> m_running
дозволяє безпечно зупинити сканування з GUI-потоку без mutex.

*// scanner.cpp --- метод run() (виконується у QThread)*\
void Scanner::run() {\
m_running.store(**true**);\
\
*// Pre-count файлів для точного QProgressBar*\
int total = 0;\
QDirIterator counter(m_directory,\
QDir::Files \| QDir::NoSymLinks, QDirIterator::Subdirectories);\
**while** (counter.hasNext()) { counter.next(); ++total; }\
\
int current = 0, threats = 0;\
QDirIterator it(m_directory,\
QDir::Files \| QDir::NoSymLinks, QDirIterator::Subdirectories);\
\
**while** (it.hasNext() && m_running.load()) {\
const QString filePath = it.next();\
emit progress(++current, total);\
\
*// Перевірка whitelist (вимога ВР5)*\
bool skip = **false**;\
**for** (const QString& wl : std::as_const(m_whitelist))\
**if** (filePath.startsWith(wl, Qt::CaseInsensitive))\
{ skip = **true**; **break**; }\
**if** (skip) **continue**;\
\
const QString md5 = computeMd5(filePath);\
**if** (md5.isEmpty()) {\
emit logMessage(\"WARNING\", \"Cannot open: \" + filePath);\
**continue**;\
}\
**if** (m_db) {\
const QString threat = m_db-\>lookup(md5);\
**if** (!threat.isEmpty()) {\
++threats;\
emit threatFound(filePath, threat, md5);\
}\
}\
}\
emit finished(current, threats);\
}

4.  **Модуль Quarantine**

Quarantine ізолює файл через QFile::rename() (атомарна операція в межах
одного тому) до директорії Quarantine/ у AppData. Метадані зберігаються
у quarantine_index.json. Перед переміщенням перевіряється вільне місце
(ризик Р3 з ТЗ).

*// quarantine.cpp --- ізоляція файлу*\
bool Quarantine::isolate(const QString& filePath,\
const QString& threatName) {\
*// Перевірка вільного місця (вимога з ризику Р3 у ТЗ)*\
QStorageInfo si(m_dir);\
**if** (si.bytesFree() \< 100LL \* 1024 \* 1024)\
**return** **false**;\
\
const QString dest = generateDest(QFileInfo(filePath).fileName());\
**if** (!QFile::rename(filePath, dest)) **return** **false**;\
\
QJsonObject obj;\
obj\[\"originalPath\"\] = filePath;\
obj\[\"quarantinedPath\"\] = dest;\
obj\[\"threatName\"\] = threatName;\
obj\[\"date\"\] = QDateTime::currentDateTime()\
.toString(\"dd.MM.yyyy HH:mm:ss\");\
m_index.append(obj);\
saveIndex();\
**return** **true**;\
}

5.  **Модуль ProcessList**

ProcessList використовує Windows API tlhelp32.h. Для кожного процесу
викликається QueryFullProcessImageNameW() з прапором
PROCESS_QUERY_LIMITED_INFORMATION, що дозволяє запит навіть до захищених
системних процесів. ERROR_ACCESS_DENIED обробляється без краша програми
(ризик Р4 з ТЗ).

*// processlist.cpp*\
QVector\<ProcessInfo\> ProcessList::snapshot() {\
QVector\<ProcessInfo\> result;\
HANDLE hSnap = CreateToolhelp32Snapshot(TH32CS_SNAPPROCESS, 0);\
**if** (hSnap == INVALID_HANDLE_VALUE) **return** result;\
\
PROCESSENTRY32W pe = {}; pe.dwSize = **sizeof**(pe);\
**if** (Process32FirstW(hSnap, &pe)) {\
**do** {\
ProcessInfo info;\
info.pid = **static_cast**\<uint32_t\>(pe.th32ProcessID);\
info.name = QString::fromWCharArray(pe.szExeFile);\
\
HANDLE hProc = OpenProcess(\
PROCESS_QUERY_LIMITED_INFORMATION, FALSE, pe.th32ProcessID);\
**if** (hProc) {\
wchar_t buf\[MAX_PATH\] = {}; DWORD sz = MAX_PATH;\
**if** (QueryFullProcessImageNameW(hProc, 0, buf, &sz))\
info.executablePath = QString::fromWCharArray(buf);\
CloseHandle(hProc);\
}\
**if** (info.executablePath.isEmpty())\
info.executablePath = \"N/A\"; *// ERROR_ACCESS_DENIED*\
info.isSuspicious = isSuspiciousPath(info.executablePath);\
result.append(std::move(info));\
} **while** (Process32NextW(hSnap, &pe));\
}\
CloseHandle(hSnap);\
**return** result;\
}

6.  **Модуль ReportWriter**

ReportWriter формує текстовий звіт зі статистикою та списком загроз у
файл scan_YYYYMMDD_HHMMSS.txt. Метод автоматично створює проміжні
директорії через QDir::mkpath().

*// reportwriter.cpp (фрагмент)*\
bool ReportWriter::write(const QString& reportPath,\
const ScanResult& result) {\
QDir().mkpath(QFileInfo(reportPath).absolutePath());\
QFile file(reportPath);\
**if** (!file.open(QIODevice::WriteOnly \| QIODevice::Text))\
**return** **false**;\
\
QTextStream out(&file);\
out \<\< \"================================================\\n\"\
\<\< \" ANTIVIRUS MVP --- ЗВІТ СКАНУВАННЯ\\n\"\
\<\< \"================================================\\n\"\
\<\< \"Дата/Час: \"\
\<\< result.timestamp.toString(\"dd.MM.yyyy HH:mm:ss\") \<\< \"\\n\"\
\<\< \"Директорія: \" \<\< result.scannedDir \<\< \"\\n\"\
\<\< \"Файлів: \" \<\< result.totalFiles \<\< \"\\n\"\
\<\< \"Загроз: \" \<\< result.threatsFound \<\< \"\\n\"\
\<\<
\"\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\--\\n\";\
**for** (const ThreatEntry& t : result.threats)\
out \<\< \"\\n \[!\] \" \<\< t.threatName \<\< \"\\n\"\
\<\< \" \" \<\< t.filePath \<\< \"\\n\"\
\<\< \" MD5: \" \<\< t.md5 \<\< \"\\n\";\
out \<\< \"================================================\\n\";\
**return** **true**;\
}

4.  **Реалізація графічного інтерфейсу**

Клас MainWindow будує весь UI програматично (без .ui-файлу) у методах
buildScanTab(), buildProcessTab() тощо. Ключовий момент реалізації ---
інтеграція Scanner з QThread без блокування GUI-потоку. Qt автоматично
встановлює Qt::QueuedConnection для сигналів між об'єктами різних
потоків, що гарантує безпечне оновлення віджетів тільки з GUI-потоку.

*// mainwindow.cpp --- запуск Scanner у QThread*\
void MainWindow::startScan() {\
m_scanThread = **new** QThread(**this**);\
m_scanner = **new** Scanner; *// без батька --- буде moveToThread*\
m_scanner-\>setDirectory(m_dirEdit-\>text().trimmed());\
m_scanner-\>setWhitelist(loadWhitelist());\
m_scanner-\>setSignatureDB(&m_sigDB);\
m_scanner-\>moveToThread(m_scanThread);\
\
*// Qt::QueuedConnection встановлюється автоматично (cross-thread)*\
connect(m_scanThread, &QThread::started, m_scanner, &Scanner::run);\
connect(m_scanner, &Scanner::progress, **this**,
&MainWindow::onScanProgress);\
connect(m_scanner, &Scanner::threatFound, **this**,
&MainWindow::onThreatFound);\
connect(m_scanner, &Scanner::finished, **this**,
&MainWindow::onScanFinished);\
\
*// Автоматичне очищення після завершення потоку*\
connect(m_scanner, &Scanner::finished, m_scanThread, &QThread::quit);\
connect(m_scanThread, &QThread::finished, m_scanner,
&QObject::deleteLater);\
connect(m_scanThread, &QThread::finished, m_scanThread,
&QObject::deleteLater);\
\
m_scanThread-\>start();\
}

При виявленні загрози слот onThreatFound() виконується в GUI-потоці
(через QueuedConnection): додає рядок до таблиці та пропонує користувачу
помістити файл до карантину через QMessageBox::question.

5.  **База сигнатур та демонстраційний файл EICAR**

Для тестування системи використовується стандартний EICAR-тест-файл
(European Institute for Computer Anti-Virus Research). Його MD5-хеш є
публічно відомим та включений до signatures.db:

\# signatures.db --- формат: MD5_HEX ThreatName\
44d88612fea8a8f36de82e1278abb02f EICAR-Standard-Test-File

EICAR-тестовий рядок (абсолютно нешкідливий, стандартизований):

X5O!P%@AP\[4\\PZX54(P\^)7CC)7}\$EICAR-STANDARD-ANTIVIRUS-TEST-FILE!\$H+H\*

Збереження цього рядку у файл .txt або .com і запуск сканування тієї ж
директорії дозволяє перевірити правильність виявлення без використання
реального шкідливого коду.

4.  []{#Xcedc4a694333a16d765df153879a565d7d44cfa .anchor}Тестування на
    інстркція користувача

    1.  **Стратегія тестування**

        1.  **4.1.1. Мета та підхід**

Метою тестування є підтвердження відповідності реалізованого програмного
забезпечення функціональним вимогам ВР1--ВР7, сформульованим у розділі
2.1.4, та нефункціональним вимогам НВ1--НВ10. Тестування проводиться у
три рівні:

**Рівень 1 --- Unit-тестування** виконується засобами фреймворку Qt Test
(вбудованого у Qt 6). Кожен клас ядра (SignatureDB, Scanner, Quarantine)
перевіряється ізольовано. Unit-тести запускаються через ctest як частина
CMake-збірки. Ціль --- верифікація коректності бізнес-логіки незалежно
від GUI.

**Рівень 2 --- Інтеграційне тестування** виконується ручним чином.
Перевіряється взаємодія компонентів: сканер → GUI, карантин →
JSON-індекс, процеси → таблиця. Кожна перевірка відповідає одному
тест-кейсу (TC-01--TC-12) із задокументованими кроками відтворення.

**Рівень 3 --- Регресійне тестування** проводиться після виправлення
виявлених дефектів. Повторно виконуються тест-кейси для відповідних
вимог.

2.  **4.1.2. Тестове середовище**

  -----------------------------------------------------------
  Параметр                 Значення
  ------------------------ ----------------------------------
  Операційна система       Windows 11 22H2 (build 22621)

  Компілятор               MSVC 2022 (v19.38)

  Фреймворк                Qt 6.6.x

  Система збірки           CMake 3.28

  Версія збірки            AntivirusMVP v1.0.0
  -----------------------------------------------------------

3.  **4.1.3. Тестові дані**

- **EICAR-тест-файл:** рядок
  X5O!P%@AP\[4\\PZX54(P\^)7CC)7}\$EICAR-STANDARD-ANTIVIRUS-TEST-FILE!\$H+H\*
  збережений у C:\\Temp\\test_scan\\eicar_test.txt. MD5-хеш файлу
  44d88612fea8a8f36de82e1278abb02f присутній у signatures.db.

- **Заблокований файл:** для перевірки ризику Р1 (файл заблокований
  іншим процесом) використовується PowerShell-команда
  \[IO.File\]::Open() з ексклюзивним блокуванням.

- **Білий список:** C:\\Temp\\test_scan у файлі whitelist.txt для
  перевірки ВР5.

  1.  **4.1.4. Критерії успішності тестування**

- **Критичні тест-кейси** (TC-01, TC-04, TC-11): всі три повинні бути
  пройдені. Провал будь-якого означає блокуючий дефект.

- **Всі 12 тест-кейсів** повинні бути виконані та задокументовані у
  файлі docs/testing/test-cases.md.

- **Критичні та High-пріоритетні дефекти** повинні бути виправлені до
  здачі роботи.

- **Medium/Low дефекти** документуються як відомі обмеження.

  1.  **4.2. Таблиця тест-кейсів**

Повний набір тест-кейсів (TC-01--TC-12) з деталізованими кроками
відтворення збережено у файлі docs/testing/test-cases.md. Нижче наведена
зведена таблиця:

**Таблиця 4.1 --- Тест-кейси для перевірки функціональних вимог**

  ------------------------------------------------------------------------------
  ID      Вимога   Назва тест-кейсу                      Пріоритет   Результат
  ------- -------- ------------------------------------- ----------- -----------
  TC-01   ВР1      Сканування з виявленням EICAR         Критичний   ✅ Пройдено

  TC-02   ВР1      Сканування порожньої директорії       Середній    ✅ Пройдено

  TC-03   ВР1      Зупинка сканування кнопкою «Зупинити» Середній    ✅ Пройдено

  TC-04   ВР2      Ізоляція файлу до карантину           Критичний   ✅ Пройдено

  TC-05   ВР2      Відновлення файлу з карантину         Високий     ✅ Пройдено

  TC-06   ВР3      Відображення списку процесів          Високий     ✅ Пройдено

  TC-07   ВР3      Процеси без прав доступу (виводить    Середній    ✅ Пройдено
                   «N/A»)                                            

  TC-08   ВР4      Автозбереження звіту після сканування Високий     ✅ Пройдено

  TC-09   ВР5      Виключення файлу через білий список   Середній    ✅ Пройдено

  TC-10   ВР6      Журналювання всіх операцій            Середній    ✅ Пройдено

  TC-11   ВР7      Реагування GUI під час фонового       Критичний   ✅ Пройдено
                   сканування                                        

  TC-12   НВ5      Обробка заблокованого файлу без краша Середній    ✅ Пройдено
  ------------------------------------------------------------------------------

2.  **4.3. Виявлені дефекти**

У процесі тестування виявлено та задокументовано два дефекти середнього
пріоритету.

1.  **Дефект BUG-01**

**Назва:** Кнопка «Відкрити звіт» залишається неактивною після зупинки
сканування\
**Вимога:** ВР4-Звітування\
**Пріоритет:** Medium

**Очікувана поведінка:** Якщо сканування зупинено вручну та встигло
знайти принаймні один файл, кнопка «Відкрити звіт» повинна активуватися
--- частковий звіт є кориснішим за жоден.

**Фактична поведінка:** m_btnReport-\>setEnabled(true) викликається лише
у слоті onScanFinished(), який не спрацьовує при зупинці (сигнал
finished не генерується до повного завершення ітерації).

**Кроки відтворення:** 1. Розпочати сканування директорії з 50+ файлами
2. Після обробки декількох файлів натиснути «Зупинити» 3. Переконатися,
що кнопка «Відкрити звіт» залишилась сірою

**Виправлення:** Сигнал finished генерується при будь-якому завершенні
run() --- і нормальному, і по stop(). Кнопка активується у
onScanFinished() незалежно від причини завершення. Виправлення
підтверджено повторним виконанням TC-08.

2.  **Дефект BUG-02**

**Назва:** Рядки результатів сканування чистих файлів не додаються до
таблиці\
**Вимога:** ВР1-Сканування\
**Пріоритет:** Low

**Очікувана поведінка:** Таблиця результатів відображає всі перевірені
файли, позначаючи чисті як «✅ Чисто».

**Фактична поведінка:** До таблиці додаються лише рядки з загрозами (у
слоті onThreatFound()). Чисті файли не відображаються --- таблиця
виглядає порожньою при відсутності загроз.

**Кроки відтворення:** 1. Сканувати директорію без EICAR-файлу 2.
Переконатися, що таблиця порожня, хоча файли були перевірені

**Рішення:** Відображення кожного файлу у таблиці (навіть чистих)
суттєво збільшує навантаження на GUI при великих директоріях. Для MVP
прийнято рішення: відображати лише загрози; статусний рядок «Перевірено:
N Загроз: 0» достатньо інформує користувача. Дефект задокументований як
«відома відмова від функції» (known limitation) у розділі 2.1.6.2.

3.  **4.4. Інструкція користувача**

    1.  **4.4.1. Встановлення та запуск**

<!-- -->

1.  Скопіювати файли AntivirusMVP.exe, signatures.db та необхідні Qt DLL
    (Qt6Core.dll, Qt6Gui.dll, Qt6Widgets.dll) до однієї директорії.

2.  Запустити AntivirusMVP.exe подвійним кліком. Права адміністратора
    рекомендуються для повного доступу до шляхів процесів.

3.  При першому запуску програма автоматично створить директорію
    %APPDATA%\\AntivirusMVP\\ з підпапками Quarantine\\, Reports\\ та
    файлом antivirus.log.

    1.  **4.4.2. Виконання сканування**

4.  На вкладці **«Сканування»** натиснути **«Обрати...»** та вибрати
    папку для перевірки.

5.  Натиснути **«▶ Розпочати сканування»**. Прогрес-бар відображає
    поточний стан.

6.  При виявленні загрози з'явиться діалог із запитом: помістити до
    карантину чи ігнорувати.

7.  Після завершення натиснути **«Відкрити звіт»** для перегляду
    детального звіту.

    1.  **4.4.3. Управління карантином**

- Вкладка **«Карантин»** показує всі ізольовані файли.

- **«↩ Відновити»** --- повертає файл на оригінальне місце.

- **«🗑 Видалити назавжди»** --- безповоротно видаляє файл з карантину.

  1.  **4.4.4. Перегляд процесів**

- Вкладка **«Процеси»** → натиснути **«🔄 Оновити список»**.

- Рядки, виділені **червоним**, вказують на процеси з підозрілими
  шляхами (\\Temp\\, \\Downloads\\). Це не означає автоматичного
  виявлення загрози --- рішення приймає користувач.

  1.  **4.4.5. Налаштування білого списку**

- Вкладка **«Налаштування»** → ввести повні шляхи директорій (один на
  рядок), які слід виключити зі сканування.

- Натиснути **«Зберегти білий список»**. Налаштування застосовуються при
  наступному скануванні.

  1.  **4.4.6. Оновлення бази сигнатур**

Замінити файл signatures.db поруч з виконуваним файлом. Формат рядка:

MD5_HEX ThreatName

Програма завантажує базу при кожному старті. Для отримання актуальних
сигнатур можна використовувати відкриту базу ClamAV або сформувати
власну.

[]{#_Toc231256455 .anchor}Висновки

У ході виконання курсового проєкту розроблено програмне забезпечення для
захисту від вірусів та шкідливих програм --- «Antivirus MVP» ---
відповідно до технічного завдання, сформульованого у розділі 2.1.

**Виконані завдання:**

1.  Розроблено модуль сигнатурного аналізу на базі
    QCryptographicHash::Md5 та QDirIterator; реалізовано завантаження
    бази хешів у QHash\<QString,QString\> з O(1)-пошуком.

2.  Реалізовано перегляд активних процесів системи через Windows API
    CreateToolhelp32Snapshot() / QueryFullProcessImageNameW() з
    коректною обробкою ERROR_ACCESS_DENIED.

3.  Розроблено модуль карантину: ізоляція файлів через QFile::rename(),
    збереження метаданих у quarantine_index.json, відновлення та
    видалення.

4.  Реалізовано п'ятивкладковий графічний інтерфейс на Qt 6 Widgets
    (QMainWindow, QTabWidget); тривале сканування виконується у QThread,
    GUI залишається реагуючим.

5.  Розроблено потоко-безпечний Singleton-логер (QMutex, RAII),
    генератор текстових звітів та підтримку білого списку виключень.

6.  Підготовлено демонстраційну базу сигнатур із EICAR-тестовим файлом.

**Технічні характеристики реалізованої утиліти:**

  -----------------------------------------------------------------------
  Параметр                            Значення
  ----------------------------------- -----------------------------------
  Мова реалізації                     C++ 17

  Фреймворк                           Qt 6 Widgets (LGPL v3)

  Архітектура                         Статична бібліотека ядра +
                                      GUI-виконуваний файл

  Кількість класів                    7 (Scanner, SignatureDB,
                                      Quarantine, ProcessList, Logger,
                                      ReportWriter, MainWindow)

  Метод виявлення                     MD5-сигнатурний аналіз

  Тестове покриття                    12 тест-кейсів (ВР1--ВР7, НВ5)

  Виявлені дефекти                    2 (обидва задокументовані та
                                      виправлені/обгрунтовані)

  Цільова ОС                          Windows 10 версія 2004+ / Windows
                                      11
  -----------------------------------------------------------------------

Розробку організовано за Scrum-методологією: 14 User Stories, 3 спринти,
19 Story Points. Управління версіями --- Git Gitflow, репозиторій
опублікований на GitHub.

**Академічна доброчесність.** Архітектурні рішення, структура класів та
логіка алгоритмів розроблені автором самостійно. GitHub Copilot (модель
Claude Sonnet 4.6) використовувався як асистент: генерація шаблонного
коду Qt (підключення сигналів/слотів, компонування віджетів), шаблонів
документації (User Stories, тест-кейси) та рефакторинг фрагментів коду.
Весь вміст пояснювальної записки, технічне завдання та архітектурні
рішення опрацьовано та перевірено автором.

[]{#_Toc231256457 .anchor}Список використаної літератури

1\. Qt Documentation. Qt 6.6 Reference Documentation. - The Qt Company,
2023. - URL: https://doc.qt.io/qt-6/ (дата звернення: 07.06.2026).

2\. Qt Documentation. QCryptographicHash Class. - The Qt Company, 2023.
--- URL: https://doc.qt.io/qt-6/qcryptographichash.html (дата звернення:
07.06.2026).[]{#_Toc231256458 .anchor}

3\. Microsoft. CreateToolhelp32Snapshot function (TlHelp32.h). -
Microsoft Learn, 2024. - URL:
https://learn.microsoft.com/en-us/windows/win32/api/tlhelp32/nf-tlhelp32-createtoolhelp32snapshot
(дата звернення: 07.06.2026).

4\. EICAR - European Institute for Computer Antivirus Research.
Anti-Malware Testfile. - EICAR, 2003. - URL:
https://www.eicar.org/download-anti-malware-testfile/ (дата звернення:
07.06.2026).

5\. ClamAV Documentation. ClamAV® open source antivirus engine. ---
Cisco Systems, 2024. --- URL: https://docs.clamav.net/ (дата звернення:
07.06.2026).

6\. Sutherland J., Schwaber K. The Scrum Guide. The Definitive Guide to
Scrum: The Rules of the Game. - Scrum.org, 2020. - 13 с. - URL:
https://scrumguides.org/docs/scrumguide/v2020/2020-Scrum-Guide-Ukrainian.pdf
(дата звернення: 07.06.2026).

7\. Stroustrup B. The C++ Programming Language. 4th ed. - Addison-Wesley
Professional, 2013. --- 1376 с.

8\. Atlassian. Gitflow Workflow. - Atlassian Bitbucket Documentation,
2024. --- URL:
https://www.atlassian.com/git/tutorials/comparing-workflows/gitflow-workflow
(дата звернення: 07.06.2026).

9\. Microsoft. QueryFullProcessImageNameW function (winbase.h). ---
Microsoft Learn, 2024. - URL:
https://learn.microsoft.com/en-us/windows/win32/api/winbase/nf-winbase-queryfullprocessimagenamew
(дата звернення: 07.06.2026).

10\. ДСТУ 3974-2000. Системи технологій. Правила виконання
техніко-економічного обґрунтування. - Держстандарт України, 2000.

Додатки

Додаток А. Вихідний код програми
